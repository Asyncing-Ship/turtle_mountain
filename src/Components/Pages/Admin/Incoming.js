import React, { Component } from 'react';

class Incoming extends Component {
  render() {
    return (
      <>
        These are incoming user requests:
      </>
    )
  }
}

export default Incoming;